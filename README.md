# html_to_jupyter_notebooks_batch_converter
The script massively convert all the html files into Jupyter notebooks (.ipynb) that are found under a given root folder and save them in their native path
